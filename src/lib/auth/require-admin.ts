/** @deprecated Import from `@/lib/auth` — kept for existing import paths. */
export {
	ADMIN_REQUIRED_MESSAGE,
	OWNER_REQUIRED_MESSAGE,
	UNAUTHENTICATED_MESSAGE,
	requireAdminActor,
	requireOwnerActor,
} from "@/lib/auth";
